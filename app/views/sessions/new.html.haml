= form_for :session, url: sessions_path do |f|

  = f.label :email, 'Email'
  = f.email_field :email,
    maxlength: 64

  = f.label :password, 'Password'
  = f.password_field :password,
    maxlength: 64

  = f.submit 'Login'
