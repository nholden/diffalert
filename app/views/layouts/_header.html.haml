.header

  .logo
    .icon= link_to "&#128680;".html_safe, root_path
    .wordmark= link_to "DiffAlert", root_path

  .menu
    - if current_user
      = link_to 'Log out', session_path, method: :delete
    - else
      #{link_to 'Sign up', new_user_path} | #{link_to 'Log in', new_session_path}
